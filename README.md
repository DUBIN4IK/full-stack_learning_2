# CSS Selectors and Specificity Showcase

This project demonstrates various CSS selectors, their application, and specificity calculation rules based on HTML5 standards.

## Technologies
* HTML5 (W3C validated)
* CSS3 (Selectors & Specificity)

## Usage
Open `index.html` in any web browser to view the HTML/CSS selectors showcase.